spring:
  application:
    name: SentinelITSM
  datasource:
    url: jdbc:postgresql://localhost:5050
    username: postgres
    password: 1234

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

  sql:
    init:
      mode: always
